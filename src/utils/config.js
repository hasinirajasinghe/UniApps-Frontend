// Replace all the places where the defined url is typed out with this variable instead
// so that it can be changed in all places by changing this variable
const config = {
    BACKEND_BASE_URL: "https://uni-apps.herokuapp.com"
}

export default config